{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run C++ (F5)",
            "type": "shell",
            "command": "konsole",
            "args": [
                "-e",
                "bash",
                "-c",
                "clang-format -i '${file}' && mkdir -p '${fileDirname}/ejecutables' && g++ '${file}' -o '${fileDirname}/ejecutables/${fileBasenameNoExtension}' && '${fileDirname}/ejecutables/${fileBasenameNoExtension}'; read -p 'Presiona ENTER para cerrar...'"
            ],
            "problemMatcher": []
        }
    ]
}
